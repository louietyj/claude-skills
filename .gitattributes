# Normalise to LF in the repo. integration_test.sh and cfs.py both run in the
# Linux code-execution sandbox, where CRLF line endings break a shell script's
# shebang and interpreter dispatch.
* text=auto eol=lf
*.sh text eol=lf
*.py text eol=lf
*.mjs text eol=lf
